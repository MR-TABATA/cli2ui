__pycache__/
*.py[cod]
db.sqlite3
.env
*.log
.DS_Store
.venv/
venv/
.playwright-mcp/
# Demo database dump — fetched on demand by seed/demo/fetch.sh, never committed.
/seed/demo/*.sql
/seed/demo/*.zip
/seed/demo/.extract/
/*.png
*.code-workspace
# Developer build/live-reload override — copied from the .example, kept local.
docker-compose.override.yml

# Local, never-committed working area (strategy notes, drafts, scratch).
/local/
.claude/
.continue/